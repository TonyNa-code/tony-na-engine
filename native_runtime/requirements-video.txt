# Optional experimental native video frame backend.
#
# The default runtime uses the system player bridge and does not require this file.
# Install this only when evaluating future in-window video frame preview support.
opencv-python>=4.9,<5
