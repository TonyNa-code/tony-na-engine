# Optional experimental native video backend.
#
# The default runtime uses the system player bridge and does not require this file.
# Install this when evaluating in-window OpenCV video frame preview / visual playback.
opencv-python>=4.9,<5
